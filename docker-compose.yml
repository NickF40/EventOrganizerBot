version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      CONFIG_FILE: /app/config.yaml
      PYTHONPATH: /app
    volumes:
      - .:/app
      - ./config.yaml:/app/config.yaml:ro
    command: ["uv", "run", "python", "-m", "app.main"]
